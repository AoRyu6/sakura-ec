%h1.fs-4 配送先住所一覧

= link_to '新しいお届け先住所を登録', new_shipping_address_path, class: 'nav-link text-primary'

- @shipping_addresses.each do |shipping_address|
  .mt-2.card
    .card-body
      .card-title.fw-bold= shipping_address.recipient_name
      .card-text #{shipping_address.postal_code} #{shipping_address.city}
      %div
        = link_to '配送先を編集する', edit_shipping_address_path(shipping_address), class: 'nav-link text-primary'
        = button_to '配送先を削除する', shipping_address, method: :delete, data: { turbo_confirm: '本当に削除しますか？' }, class: 'nav-link text-black'
